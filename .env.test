# OTAP — Test (T)
# Vitest unit/integratie-tests + Cypress E2E
# Aparte SQLite-database zodat testdata de dev-db niet vervuilt

DATABASE_URL="file:./test.db"
JWT_SECRET="test-jwt-secret-32-chars-minimum!!"
CORS_ORIGIN="http://localhost:5173"
PORT=3001
NODE_ENV=test
